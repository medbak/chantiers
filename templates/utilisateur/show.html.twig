{% extends 'base.html.twig' %}

{% block title utilisateur.nom %}



{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <p></p>
            </div>
            <div class="col-md-4">
                <p></p>
            </div>
        </div>

        <p></p>

        <div class="row">
            <div class="col-md-8">
                <h2>Détails Utilisateur</h2>
                <table class="table table-striped table-bordered table-hover table-checkable">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>Prenom</th>
                        <th>Matricule</th>

                    </tr>
                    </thead>

                    <tbody>
                    <tr>
                        <td>{{ utilisateur.id }}</td>
                        <td>{{ utilisateur.nom }}</td>
                        <td>{{ utilisateur.prenom }}</td>
                        <td>{{ utilisateur.matricule }}</td>
                    </tr>

                    </tbody>

                </table>
                <div class="text-right">
                    <a href="{{ path('utilisateur.index')}}" class="btn btn-danger">Retour</a>
                </div>

            </div>
            <div class="col-md-4">

            </div>
        </div>

    </div>
{% endblock body %}



